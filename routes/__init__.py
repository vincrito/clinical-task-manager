# This file tells Python that "routes" is a package (so imports work).
# We also re-export blueprints here if needed later.

# Example:
# from .main import bp as main_bp  # import the "main" blueprint
# __all__ = ["main_bp"]            # optional: controls what gets exported on *
